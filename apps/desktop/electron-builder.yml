appId: com.mediaautopublish.desktop
productName: 可达AI
productFilename: 可达AI
icon: build/icon.png

directories:
  output: release

files:
  - "dist/**"
  - "package.json"

asar: true

win:
  signAndEditExecutable: false
  target:
    - target: nsis
  artifactName: "keduckAI-${version}-${os}-${arch}.${ext}"

mac:
  target:
    - target: dmg
    - target: zip
  artifactName: "${productName}-${version}-${os}-${arch}.${ext}"
  category: public.app-category.productivity

linux:
  target:
    - target: AppImage
  category: Utility
  artifactName: "${productName}-${version}-${os}-${arch}.${ext}"

nsis:
  oneClick: false
  perMachine: false
  allowToChangeInstallationDirectory: true

publish:
  provider: github
  owner: "${env.GH_OWNER}"
  repo: "${env.GH_REPO}"
